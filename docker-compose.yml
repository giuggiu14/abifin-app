services:
    db:
      image: postgres:17
      ports:
        - "5432:5432"
      environment:
        POSTGRES_DB: '${DB_DATABASE}'
        POSTGRES_USERNAME: '${DB_USERNAME}'
        POSTGRES_PASSWORD: '${DB_PASSWORD}'